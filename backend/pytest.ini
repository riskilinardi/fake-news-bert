[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
markers =
    integration: tests that load the real model (slow, opt-in via RUN_INTEGRATION=1)
filterwarnings =
    ignore::DeprecationWarning
